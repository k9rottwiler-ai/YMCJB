# Copy to .env.local for local overrides (not committed).

# In-app unlock password (Vite build-time). Empty = no app login gate.
VITE_APP_PASSWORD=

# Railway / Caddy HTTP basic auth (runtime — no rebuild needed to change).
# APP_USERNAME=admin
# APP_PASSWORD=
